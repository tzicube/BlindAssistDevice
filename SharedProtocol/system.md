# ESP 
ESP Device  // operating two streams in parallel
ESP Device Startup 
│
├── Task 1: Video Streaming
│   └── Continuously captures and sends JPEG frames to the server
│
└── Task 2: User Interaction
    ├── Monitors the button state
    ├── Records user audio when the button is pressed
    ├── Sends a request to /api/recognize
    ├── Sends a request to ///
    ├── Sends gps to  //
    └── Receives an audio/wav response from the server
